package com.farart.snippy.repositories;

import com.farart.snippy.entities.ComplementaryField;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ComplementaryFieldsRepository extends JpaRepository<ComplementaryField, Long> {
}
